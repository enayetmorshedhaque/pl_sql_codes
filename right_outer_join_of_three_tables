-- query for right outer join

SELECT A.CUSTOMER_ID, A.NAME, A.ADDRESS, A.CREDIT_LIMIT, B.EMAIL, B.PHONE, C.ORDER_DATE, C.STATUS
FROM ORDERS C
RIGHT OUTER JOIN CONTACTS B
ON C.CUSTOMER_ID = B.CUSTOMER_ID
RIGHT OUTER JOIN CUSTOMERS A
ON B.CUSTOMER_ID = A.CUSTOMER_ID
ORDER BY CUSTOMER_ID ASC;
